import 'package:flutter/material.dart';

class BiometricLoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Biometric Login Screen')),
    );
  }
}
